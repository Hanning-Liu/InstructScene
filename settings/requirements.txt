# PyTorch: install separately (see settings/setup.sh). On Apple Silicon use the
# official macOS wheels from pytorch.org (MPS); do not use the Linux CUDA index.
# If you later add faiss, use the CPU build on Mac: pip install faiss-cpu
# `hf` CLI + Hub API; keep <1.0 so `transformers` stays compatible.
huggingface_hub>=0.34,<1.0
scipy
imageio
trimesh
shapely
clean-fid
tensorboard
nltk
einops
transformers
diffusers
